/**
 * Max `found` from hh.ru vacancy search for heavy client work (multi-page fetch, full-detail save).
 * Above this, narrow filters first — title chips then only filter the current list page.
 */
export const HH_VACANCY_SEARCH_MAX_FOUND_FOR_HEAVY_ACTIONS = 400;
